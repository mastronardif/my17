import { MystoreDirective } from './mystore.directive';

describe('MystoreDirective', () => {
  it('should create an instance', () => {
    const directive = new MystoreDirective();
    expect(directive).toBeTruthy();
  });
});
